name: chrome_extension
version: 0.5.0
description: A library for accessing the `chrome.*` APIs available in Chrome extensions.
homepage: https://github.com/xvrh/chrome_extension.dart
environment:
  sdk: '>=3.7.0 <4.0.0'

funding:
  - https://www.buymeacoffee.com/xvrh
  - https://github.com/sponsors/xvrh

dependencies:
  web: '>=0.5.0 <2.0.0'

dev_dependencies:
  analyzer:
  build_runner:
  checks:
  code_builder:
  collection:
  dart_style:
  fetch_client:
  http:
  json_annotation:
  json_serializable:
  lints:
  mockito:
  path:
  petitparser:
  process_runner:
  project_tools:
    git:
      url: https://github.com/xvrh/project_tools.dart.git
  pub_semver:
  puppeteer:
  shelf:
  shelf_router:
  shelf_static:
  test:
  yaml:
